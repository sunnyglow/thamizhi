<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.vayalum.vazhvum.thamizhi">

    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".InputReadActivity"
            android:label="@string/title_activity_input_read"
            android:theme="@style/AppTheme.NoActionBar"
            tools:ignore="Instantiatable"></activity>
        <activity
            android:name=".ExtendedReadModeActivity"
            android:label="@string/title_activity_extended_read_mode"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".BrowseModeActivity"
            android:label="@string/title_activity_browse_mode"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".DashboardActivity"
            android:label="@string/title_activity_dashboard"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-5540165822417974~2245193969" />
    </application>

</manifest>